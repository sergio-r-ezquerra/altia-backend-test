version: '3.8'

services:
  backend:
    build: .
    container_name: app-prices-container
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
